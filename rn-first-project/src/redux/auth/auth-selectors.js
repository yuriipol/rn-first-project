export const errMessage = (state) => state.auth.error;
export const isLogin = (state) => state.auth.isLogIn;
export const dataUser = (state) => state.auth.user;
export const isLoad = (state) => state.auth.isLoading;
